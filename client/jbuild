(executables
 ((names (build_pkg))
  (libraries (
    async
    build_pkg_common
    memo_cache
    ocaml_re
    public_release_lib))
  (preprocess (pps (ppx_jane ppxlib.runner))))
 )


(jbuild_version 1)
