(executables
 ((names (build_pkg))
  (libraries (
    core_extended
    public_release_lib
    build_pkg_common
    ocaml_re
    async))
  (preprocess (pps (ppx_jane))))
 )

(jbuild_version jane_street)
